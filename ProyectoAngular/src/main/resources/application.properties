spring.application.name=ProyectoAngular

# =========================
# Conexión a PostgreSQL
# =========================
spring.datasource.url=jdbc:postgresql://localhost:5432/agrolink_db
spring.datasource.username=postgres
spring.datasource.password=juanocsa12345
spring.datasource.driver-class-name=org.postgresql.Driver

# =========================
# JPA / Hibernate
# =========================

spring.jpa.hibernate.ddl-auto=update

# Muestra las consultas SQL en consola (útil para debug)
spring.jpa.show-sql=true

# Dialecto específico de PostgreSQL
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect